{
  "name": "typescript-pwa-with-vite-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:sw": "tsc -p tsconfig.sw.json",
    "dev": "npm run build:sw && vite",
    "build": "npm run build:sw && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/serviceworker": "^0.0.108",
    "sass": "^1.83.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.5",
    "vite-plugin-pwa": "^0.21.1"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
